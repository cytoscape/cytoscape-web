# Cytoscape Web

## Requirements

- yarn 1.22.11
- Node.js 16.8.0

## Setup

run `yarn install`

## Commands

- `build`: build the app for production
- `dev`: run a dev server that watches code changes
