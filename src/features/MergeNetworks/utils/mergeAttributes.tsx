import { IdType } from "../../../models/IdType";
import { ValueType } from "../../../models/TableModel";
import { MatchingTable } from "../model/Impl/MatchingTable";

export function mergeAttributes(networkId: IdType, goAttMap: Map<string, Record<string, ValueType>>, attributeMapping: MatchingTable) {

    return {};
}