import { ServiceApp } from '../../../models/AppModel/ServiceApp'

export const createMenu = (serviceApp: ServiceApp) => {
  const { cyWebMenuItem } = serviceApp
  const { root, path } = cyWebMenuItem
}
