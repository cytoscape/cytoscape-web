// import { ValueType } from './Table'
// import { VisualProperty } from './NetworkView'

// type RGB = `rgb(${number}, ${number}, ${number})`
// type RGBA = `rgba(${number}, ${number}, ${number}, ${number})`
// type HEX = `#${string}`

// export type Color = RGB | RGBA | HEX

// export type ShapeType = 'rectangle' | 'ellipse' | 'triangle'

// export type LineType = 'solid' | 'dotted' | 'dashed'

// interface Shape {
//   name?: string
//   type: ShapeType
// }

// export interface VisualMappingFunction<T extends ValueType, K> {
//   map: (value: T) => K
// }

// export interface VisualStyle {
//   name: string
//   defaults: VisualProperty<any>[]
//   mappings: VisualMappingFunction<any, any>[]
// }
