import { NetworkProperty } from '../../../models/NetworkSummaryModel/NetworkProperty'

/**
 * NdexNetworkProperty is an alias for NetworkProperty.
 * It represents network properties from NDEx API.
 */
export type NdexNetworkProperty = NetworkProperty
