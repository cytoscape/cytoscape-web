{
  "name": "api-types-consumer-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Compiles the published @cytoscape-web/api-types tarball the way a real consumer would. Never compiled in place — see scripts/verify-api-types-consumer.mjs.",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "18.3.12",
    "typescript": "5.5.4"
  }
}
